// Party House — Supabase config (solo ANON key — sin secretos)
// El PAYPAL_CLIENT_ID se obtiene de /api/public-config (nunca aquí)
const SUPABASE_URL  = 'https://XXXXXXXX.supabase.co';     // reemplazar
const SUPABASE_KEY  = 'eyJhbGciOi...ANON_KEY_AQUI';       // reemplazar (anon ke